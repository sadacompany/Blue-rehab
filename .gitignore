node_modules
dist
.env
.env.local
*.log
.DS_Store
*.tsbuildinfo

# Credential files kept in the working tree. `git add -A` would otherwise commit
# them: the Twilio file sat here untracked but unignored, one command away from
# being published.
*credentials*.txt
*creidentials*.txt
*keys*.txt
*.pem
*.key

# Local Netlify folder
.netlify

# Built artefacts of the Supabase Edge Function; produced by
# scripts/build-edge-function.mjs on the way to a deploy.
supabase/functions/api/bundle.js
supabase/functions/api/dotenv-stub.mjs

.vercel
.env*
.qa
.qa_tok
